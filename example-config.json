{
	"clientId": "${CLIENT_ID}",
	"guildId": "${GUILD_ID}",
	"token": "${TOKEN}",
    "guildInfoChannelId": "${GUILD_INFO_CHANNEL_ID}",
    "applicationChannelUrl": "${APPLICATION_CHANNEL_URL}",
	"adminRoleId": "${ADMIN_ROLE_ID}"

}